<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
        body {
            text-align: center; /* Center aligns everything in the body */
            background-color: rgb(42, 42, 42); /* Light blue background */
            background-image: url('background-image-url.jpg'); /* Add your background image URL here */
            background-size: cover; /* Ensure the image covers the whole page */
            background-position: center; /* Center the background image */
            padding-top: 100px; /* Add some padding to move content down */
            color: #ffffff; /* White text color */
            font-family: Arial, sans-serif; /* Set a nice, readable font */
        }
        h1 {
            font-size: 2.5em; /* Makes the Login text bigger */
            margin-bottom: 0.5em; /* Reduce space below the title */
        }
        form {
            display: inline-block; /* Centers the form in the page */
            text-align: left; /* Aligns form elements to the left */
            margin-top: 20px; /* Adds some space above the form */
            background: rgba(32, 195, 232, 0.5); /* Semi-transparent background */
            padding: 70px; /* Add padding around the form */
            border-radius: 75px; /* Rounded corners */
        }
        input[type="text"], input[type="password"] {
            width: 100%; /* Make inputs fill the form */
            margin: 10px 0; /* Add some vertical spacing */
            padding: 10px; /* Increase input field padding */
            border-radius: 5px; /* Rounded corners for input fields */
            border: 1px solid #ddd; /* Light gray border */
        }
        input[type="submit"] {
            width: 100%; /* Make the submit button fill the form */
            padding: 10px; /* Padding for the button */
            background-color: #5C6BC0; /* Blue background color */
            color: #ffffff; /* White text color */
            border: none; /* No border */
            border-radius: 5px; /* Rounded corners */
            cursor: pointer; /* Change cursor on hover */
        }
        input[type="submit"]:hover {
            background-color: #3F51B5; /* Slightly darker blue on hover */
        }
        a {
            color: #ffffff; /* Ensure links are visible */
            text-decoration: underline; /* Underline links */
        }
    </style>
</head>
<body>
    <h1>LOGIN</h1>
 
    <form action="/login" method="post">
        <input type="text" name="username" id="username" placeholder="Username" required />
        <br />
        <input type="password" name="password" id="password" placeholder="Password" required />
        <br />
        <input type="submit" value="Submit" /><br>
        <a href="/signup">Sign Up</a>
        <p><%= locals.error ? error : '' %></p>
    </form>
</body>
</html>